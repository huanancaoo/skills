# 前端交互决策

本上下文定义前端界面设计、实现与审查时，用于选择交互载体和数据呈现方式的统一语言。决策依据来自业务任务与信息特征，不绑定具体前端框架或组件库。

## Language

**交互决策**：
在界面设计、代码实现和代码审查中，根据业务目标、任务复杂度、信息结构、数据规模和可用空间，选择界面交互与呈现方式的判断过程。该过程独立于具体框架和组件库。
_Avoid_: 组件推荐、组件套用

**交互载体**：
承载用户查看或修改业务信息的界面形态，例如 Dialog、Sheet 或独立页。
_Avoid_: 弹层组件、页面容器

**Dialog**：
暂时中断当前界面，用于完成或取消一个边界明确、无需离开当前上下文查阅信息的短时任务。其适用性由任务封闭性决定，而不是由字段数量决定。
_Avoid_: 弹窗页面、小表单容器

**Sheet**：
在保留来源页面上下文的同时，用于查看关联信息或完成局部编辑的临时工作区。来源上下文必须对当前任务持续有价值。
_Avoid_: 大号 Dialog、侧边弹窗

**独立页**：
承载主要、可单独定位或需要持续专注的界面任务。适用于需要稳定 URL、浏览历史、刷新恢复、链接分享、跨区域查阅信息、多组内容或较长编辑过程的任务。
_Avoid_: 全屏弹窗、大型 Sheet

**界面任务**：
用户为达成一个明确业务目标而执行的一段界面操作，例如创建、编辑或查看。界面任务是选择交互载体的基本单位，同一业务对象的不同界面任务可以使用不同载体。
_Avoid_: CRUD 页面、对象页面

**语义决策规则**：
按照业务风险、任务连续性、信息结构、上下文需求、操作时长和可用空间等证据，以明确优先级选择交互载体的规则。字段数量不构成独立的机械阈值，也不通过加权评分替代业务判断。
_Avoid_: 字段数阈值、组件评分

**自然字段宽度**：
与字段的预期值长度、输入方式和业务含义相称，并受当前容器可用空间约束的表单占位宽度。短值字段不默认占满整行，容器不足时可随响应式布局扩展。
_Avoid_: 固定列宽、默认整行

**字段控件**：
根据业务值的含义、允许操作、候选集合、验证约束和录入频率选择的输入或选择方式。底层数据类型不能单独决定字段控件。
_Avoid_: 类型控件、字段类型映射

**组件映射**：
在交互语义已经确定后，从当前项目的设计系统或组件库中选择能够表达该语义的具体组件。现有组件不能反向改变业务决策。
_Avoid_: 组件驱动设计、组件套用

**字段组**：
因共同表达一个业务含义或需要连续填写而形成的字段集合。字段是否并排首先由业务关系和填写顺序决定，不以填满行宽为目标。
_Avoid_: 同行字段、栅格行

**有效容器**：
表单在当前交互载体中实际可用于排版的内容区域。响应式布局以有效容器为基准，同时保持字段组关系和填写顺序。
_Avoid_: 屏幕宽度、设备宽度

**Table**：
以阅读和比较多条同类记录为主的表格呈现方式。数据集合具有明确边界，只提供基础展示和必要的行级访问。
_Avoid_: DataTable、数据列表

**DataTable**：
用于操作同类记录集合的数据工作区。用户需要排序、筛选、分页、选择、批量操作或列控制等一种或多种数据管理能力。
_Avoid_: 高级 Table、复杂表格

**DataTable 能力**：
为完成明确用户任务而独立启用的数据操作，例如排序、筛选、分页、行选择、批量操作、列控制或重排。选择 DataTable 不代表默认启用全部能力。
_Avoid_: 默认功能、完整功能

**能力确认**：
在核对需求、文档、接口和代码后，将仍会改变产品行为的未决能力直接交由用户决定。提问必须包含基于已知事实的推荐答案及理由，未确认前不启用该能力。
_Avoid_: 默认启用、无建议提问

**决策前沿**：
前置事实和依赖决策均已明确、当前可以安全询问的全部独立产品问题。同一决策前沿的问题应在一轮内提出，依赖未决答案的问题留到后续轮次。
_Avoid_: 问题清单、逐个追问

**排序**：
根据一个或多个字段计算记录的显示顺序，不修改记录本身的业务顺序。
_Avoid_: 重排、拖拽排序

**重排**：
用户修改记录在业务中的明确顺序，并将结果持久化。拖拽仅是执行重排的一种交互方式。
_Avoid_: 排序、拖拽排序

**重排范围**：
一次重排操作所作用的完整、有序记录集合。分页、筛选或部分加载后的可见记录不自动构成重排范围。
_Avoid_: 当前页、当前可见行

**数据规模基线**：
选择数据呈现方式和处理位置前必须确认的当前记录量、预期上限或增长趋势，以及服务端查询所支持的排序、筛选和分页能力。无法从权威来源确认时，该缺口会阻塞实现决策。
_Avoid_: 数据不多、以后再优化

**数据集总量**：
权威数据源中属于当前业务集合的全部记录数量，用于判断查询和服务端处理约束。
_Avoid_: 表格行数

**单次返回量**：
一次查询传输到客户端的记录数量，用于判断网络载荷和客户端内存约束。
_Avoid_: 每页显示数

**同时渲染量**：
同一时刻由界面实际创建并维护的记录行数量，用于判断渲染和交互性能约束。
_Avoid_: 数据集总量、单次返回量

**数量级**：
以十的幂次描述数据规模的粗粒度证据，用于尽早识别增长风险。数量级不能脱离数据复杂度、查询能力、载荷和用户任务而自动决定组件或优化方式。
_Avoid_: 组件阈值、性能结论

**增长型数据集合**：
没有可信的小规模上限，或记录数量预计持续增长的数据集合。其排序、筛选和分页由服务端查询承担，客户端虚拟化不能替代该边界。
_Avoid_: 大表格、以后会变多的数据
